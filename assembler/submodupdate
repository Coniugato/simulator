sudo hwclock -s
gitupdate $1
cd ~/processor-experiment-2023-5
echo "submodule fetch"
git checkout simulator
git pull origin simulator
git submodule update --remote
git add *
git commit -m "simulator submodule updated / $1"
git push origin simulator